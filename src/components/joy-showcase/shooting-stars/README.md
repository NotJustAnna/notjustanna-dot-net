# ShootingStars - Modern edition

Goals:
- More docs and comments
- Rely on timestamp instead of deltaTime
   - All stars keep their own lifetime by accumulating deltaTime instead of
        relying on the timestamp.